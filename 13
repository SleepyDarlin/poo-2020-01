//Classe 1:
public class Conta{
	// abaixo tem umas funções só pra completar o pensamento
	String Cliente;
	int nrConta;
	double Saldo;	
	String getCliente() {
		return Cliente;
	};
	void setCliente(String nomecliente) {
		this.Cliente = nomecliente ;
	};
	int getNrConta() {
		return nrConta;
	};
	double getSaldo() {
		return Saldo;
	};
	void sacar(double valor) {
		Saldo = (Saldo - valor);	
	};
	void debitar(double valor) {
		Saldo = (Saldo - valor);
	};
	double temSaldo() {
		return Saldo;
	};
	void depositar(double valor) {
		Saldo = (Saldo + valor);
	};
	void transferir() {	
	};
	void abrirConta() {	
	};
}

//Classe 2:
public class ContaEspecial extends Conta {
		double limite;
		// abaixo tem umas funções só pra completar o pensamento
		void setLimite(double numSet) {
			limite = numSet;
		};
		double getLimite() {
			return limite;
		};
		void abrirConta() {	
		};
}

//Classe 3:
public class ContaPoupança extends Conta {
		double txCorrecao;
		// abaixo tem umas funções só pra completar o pensamento
		double getTaxaCorrecao() {
			return txCorrecao;
		};
		void setTaxaCorrecao(double txSet) {
			txCorrecao = txSet;
		};
		void atualizaSaldoRendimento() {	
		};
		void abrirConta() { 
		};
}
